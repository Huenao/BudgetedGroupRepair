# Budgeted Group Repair（No-Baran Prompt）：研究背景、动机与问题定义

## 1. 研究背景

结构化数据修复通常先定位错误单元格，再为每个错误生成候选值。传统修复器能够利用共现、值域、格式和约束信号，调用成本低且结果稳定，但在长尾语义和复杂上下文上能力有限；大语言模型能够利用行上下文和列语义，却带来 token/API 成本、输出不稳定和错误覆盖风险。

本研究关注一个明确场景：错误坐标已经由数据集给定，系统需要在固定预算内决定哪些单元格交给 LLM、哪些保留传统修复结果。基础修复器使用 Baran，LLM 固定为 DeepSeek V4 Flash。

与逐单元格调用不同，Budgeted Group Repair（BGR）允许一次 query 同时包含多个目标单元格。这样可能摊薄 system prompt、schema、数据集描述和公共上下文的固定成本，也可能让相近错误在同一上下文中相互提供模式信息；但组过大或组内目标不相干时，也会产生 batch interference。因此，核心问题不是简单地“是否 batching”，而是：

> 如何生成可执行的重叠组查询，预测每个 cell 在每个 query 中相对 Baran 的增益，并在 token/API 预算下选择一组查询，使最终修复质量最大化？

## 2. No-Baran Prompt 信息边界

“No-Baran Prompt”不表示系统完全弃用 Baran，而是将 Baran 与 LLM 的信息通道分离：

- Baran 输出可以用于候选组特征、uplift 预测、预算选择、查询后的 verifier 和最终 fallback；
- Baran candidate、support、diagnostics 和 correctness 不得进入发送给 LLM 的 system/user messages；
- LLM Prompt 只包含 dirty table 可导出的内容、目标位置、masked row、相似 dirty rows、列 profile、公开 FD 组件和输出 schema；
- clean value、oracle correctness、error annotation 和 TableEG 私有标注始终不能进入查询生成阶段；
- clean value 只在响应账本冻结后用于离线评估。

该设计用于检验 LLM 在不受 Baran 候选锚定时的独立修复能力，并使 Baran–LLM 互补性能够被清楚测量。

## 3. 基本设定

给定 dirty table (D) 和已知错误单元格集合 (E)。每个 cell (i\in E) 具有 dirty value (x_i)，传统修复器给出预测 (b_i)。真实 clean value (y_i) 仅供离线评价，在线阶段不可见。

候选查询集合记为 (Q)。每个 query (q\in Q) 是不可变动作：

\[
q=(G_q,v_q,P_q,c_q),
\]

其中：

- (G_q\subseteq E) 是该 query 覆盖的目标 cells；
- (v_q\in\{\text{singleton,row,pattern,public\_fd,semantic}\}) 是构组视角；
- (P_q) 是冻结的 No-Baran Prompt；
- (c_q>0) 是实际或预估 token/API 成本。

一个 cell 可以出现在多个重叠候选 query 中。系统最终选择 (S\subseteq Q)，且满足预算约束：

\[
\sum_{q\in S}c_q\le B.
\]

未被有效 LLM 结果接管的 cell 使用 Baran fallback。

## 4. v1 候选分组

v1 保留所有 singleton，并从四个视角生成多单元格候选组。候选 group size 为 (2,4,8)，正式实验二固定使用 (k=4)。

### 4.1 Row view

同一 dirty row 中的多个已知错误单元格进入同一 bucket。该视角利用共享的 tuple context，适合一行内多个字段共同异常的情况。

### 4.2 Pattern view

同一目标列内，根据 dirty value 类型、格式签名以及 Prompt 外的 Baran 类型/支持度特征构造稀疏向量，以 cosine distance 做确定性 average-linkage 排序，再取固定宽度窗口形成候选组。

### 4.3 Public-FD view

根据公开 functional dependencies 构造 dirty-table violation components。落在同一违反组件内的 sampled error cells 形成 bucket，再生成固定大小窗口。公开 FD 及其组件可以进入 Prompt；Baran 记录不能进入 Prompt。

### 4.4 Semantic view

同一目标列内，根据 masked row text 和单元格上下文形成语义稀疏表示，通过相同的确定性 average-linkage 与窗口规则生成候选组。

四种视角产生的是重叠动作库，而不是预先确定的最终 partition。Primary partition 仅用于实验二的公平比较；完整 BGR 仍可从重叠候选中做预算选择。

## 5. 为什么需要分组

分组具有三项潜在价值：

1. **成本摊销：** 多个目标共享 system prompt、dataset schema、公共 FD 描述和 API 固定开销；
2. **上下文共享：** 相同行、相同模式、相同 FD 组件或相似语义的目标可能帮助模型识别共同规律；
3. **组合选择：** 一个 query 可以同时覆盖多个 cell，形成预算优化中的集合动作，而不是相互独立的点动作。

分组并不保证提高准确率。过强的共享上下文可能诱发复制、串扰或注意力竞争，因此本研究同时比较 structured group、相同 cell 的 singleton 以及 matched random batching。只有当 structured group 达到预注册的质量与成本门槛时，才能认为 grouping 得到支持。

## 6. Cell-query uplift

LLM 的价值依赖于 cell 与 query 的组合。对 (i\in G_q)，定义相对于 Baran 的正确性差：

\[
\Delta_{iq}=\mathbf 1[\hat y_{iq}=y_i]-\mathbf 1[b_i=y_i].
\]

将其拆为：

\[
h_{iq}=\mathbf 1[b_i\ne y_i,\hat y_{iq}=y_i],\qquad
r_{iq}=\mathbf 1[b_i=y_i,\hat y_{iq}\ne y_i].
\]

其中 (h_{iq}) 表示 helpful rescue，(r_{iq}) 表示 harmful overwrite。系统学习二者的概率，并构造保守非负收益下界 (ell_{iq})。这使同一 cell 在不同 group 中可以具有不同价值，也允许模型显式学习 batch interference。

## 7. 预算化目标

对选择集合 (S)，cell (i) 获得被选查询中的最佳保守收益：

\[
F(S)=\sum_{i\in E}w_i\max\left(0,\max_{q\in S:i\in G_q}\ell_{iq}\right).
\]

这里的 max-over-query 避免同一 cell 被重复计益，也表达系统只需为该 cell 采用最有把握的有效结果。优化问题为：

\[
\max_{S\subseteq Q}F(S)
\quad\text{s.t.}\quad
\sum_{q\in S}c_q\le B.
\]

该目标是单调次模函数，预算约束为 knapsack；因此可以使用基于边际收益/成本的 lazy greedy，并与 best singleton 或小规模精确解比较。

## 8. 研究问题

- **RQ1 互补性：** No-Baran singleton LLM 与 Baran 是否修对不同的 cells？
- **RQ2 分组质量：** structured group 相对相同 cells 的 singleton 是否提高或保持 accuracy？
- **RQ3 分组归因：** structured group 是否优于保持组大小和主要负载特征的 matched random group？
- **RQ4 成本效率：** group query 是否显著降低 token/cell 与物理 API 请求数？
- **RQ5 可路由性：** 在 family holdout/cross-fitting 下，是否能预测 helpful 与 harmful cell-query 结果？
- **RQ6 预算优化：** 次模 knapsack 选择是否在相同预算下优于逐 cell 路由和简单启发式？

## 9. 成功与失败边界

实验二采用冻结的 A/B/C 判定：

- **A：** structured quality 显著优于 singleton；
- **B：** structured 在 1 个百分点非劣界内成立，且 token/cell 至少节省 15%；
- **C：** 不满足 A 或 B。

此外，parse-validity 最多下降 1 个百分点，missing-item rate 最多增加 1 个百分点。任何 abstain、missing、invalid 或 provider failure 都留在完整分母中并按未修对处理。

若 grouping 为 C，则只能说明当前 v1 的候选生成与 Prompt 组合未获得总体支持；不能以成本节省替代质量门槛，也不能继续执行依赖 grouping 成立的 Phase 3。

## 10. 主要贡献定位

本研究的贡献集中在：

1. 将 group query 作为可重叠、可定价、可选择的修复动作；
2. 定义 query-dependent 的 helpful/harmful uplift，而不是为每个 cell 估计单一 LLM 收益；
3. 用 max-over-query 次模目标统一重叠覆盖、预算约束与 fallback；
4. 将 Baran 与 LLM Prompt 的信息通道分离，减少候选锚定并支持严格泄漏审计；
5. 通过 singleton、structured 与 matched random 三臂实验分别测量准确率、分组结构和成本摊销。

## 11. 相关工作

- Mahdavi, M. et al. **Raha: A Configuration-Free Error Detection System.** SIGMOD 2019. [论文](https://doi.org/10.1145/3299869.3324956)
- Mahdavi, M. et al. **Baran: Effective Error Correction via a Unified Context Representation and Transfer Learning.** PVLDB 2020. [论文](https://doi.org/10.14778/3415478.3415548)｜[代码](https://github.com/BigDaMa/raha)
- Rekatsinas, T. et al. **HoloClean: Holistic Data Repairs with Probabilistic Inference.** PVLDB 2017. [论文](https://www.vldb.org/pvldb/vol10/p1190-rekatsinas.pdf)｜[代码](https://github.com/HoloClean/holoclean)
- Chu, X. et al. **KATARA: A Data Cleaning System Powered by Knowledge Bases and Crowdsourcing.** SIGMOD 2015. [论文](https://doi.org/10.1145/2723372.2749431)
- Khayyat, Z. et al. **BigDansing: A System for Big Data Cleansing.** SIGMOD 2015. [论文](https://doi.org/10.1145/2723372.2747646)

这些工作分别支撑传统修复、约束/共现特征和规则化清洗背景；本文的独立重点是预算约束下的重叠组查询、cell-query uplift 与次模选择。

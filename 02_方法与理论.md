# Budgeted Group Repair（No-Baran Prompt）：方法与理论

## 1. 方法总览

方法由四个连续阶段组成：

1. **Phase 0：前置验证。** 固定数据、Baran reference、抽样、Prompt schema、模型和统计阈值；
2. **Phase 1：互补性。** 用 singleton query 测量 Baran 与独立 LLM 候选的正确集合差异；
3. **Phase 2：分组有效性。** 比较 structured group、canonical singleton 和 matched random group；
4. **Phase 2.5–3：路由与预算修复。** Router-v2 使用独立的 TableEG calibration 训练 uplift gate，再执行次模 knapsack 选择、外部 verifier 与 Baran fallback。实验一、二的效果判定保留为诊断结果，不再作为 Phase 3 的前置效果门禁。

No-Baran 仅约束 LLM messages。Baran 仍可在 Prompt 外参与签名、收益估计、选择、验证和 fallback。

## 2. 安全数据表示

在线阶段使用物理隔离的数据结构：

\[
\texttt{SafeCell}=(\text{suite,dataset,row,col,column,row\_id,dirty\_value}).
\]

该结构不包含 clean value、correctness、error type 或其他 oracle annotation。`SafeDataset` 只暴露 dirty dataframe 和安全 cells。评价阶段另行加载 `OracleCell`，并只在 action/response ledger 冻结后绑定 clean value。

对于任何暴露给 LLM 的 row：

- 当前 target 替换为 `<TARGET_ERROR_MASKED>`；
- 同一 sampled error set 中的其他错误坐标替换为 `<OTHER_DETECTED_ERROR_MASKED>`；
- 目标列 profile 统计排除 sampled error rows；
- 相似 dirty rows 不能使用目标列已知错误位置作为参考值。

## 3. Cell signature

每个 cell 的外部签名由四类信息构成：

\[
s_i=(s_i^{dirty},s_i^{row},s_i^{baran},s_i^{constraint}).
\]

### 3.1 Dirty value 特征

- column identity；
- coarse value type：empty、number、date、time、email、URL、boolean、text；
- 字母/数字/空白/标点的压缩格式签名；
- dirty value 与 row context 的稀疏 token 特征。

### 3.2 Row context 特征

同一 sampled error set 中的错误坐标统一 mask，再将列名和值组成 `masked_row_text`。这部分不读取 clean table。

### 3.3 Baran 外部特征

允许读取 fresh Baran prediction 的类型、格式和白名单 support diagnostics。这些字段可用于 pattern/semantic vector、group features 和 gate，但在 Prompt 构造前被物理丢弃。

### 3.4 Constraint 特征

只使用 `configs/public_fds.json` 中公开注册的 FD。由 dirty table 构造 violation components，并记录 rule ID、component ID、rows 和 sampled target cell IDs。

## 4. 重叠候选动作库

动作集合为：

\[
Q=Q_{single}\cup Q_{row}\cup Q_{pattern}\cup Q_{fd}\cup Q_{semantic}.
\]

### 4.1 Singleton

对每个 sampled cell (i) 生成且只生成一个 singleton action：

\[
G_q=\{i\},\quad v_q=\text{singleton}.
\]

它既是独立基线，也是完整 BGR 的合法候选动作。

### 4.2 Row groups

按 row index 分 bucket。同一 row 中至少两个 sampled error cells 时，按稳定 cell ID 顺序生成固定宽度窗口。

### 4.3 Pattern groups

按 target column 分 bucket。对 cell (i) 构造 pattern vector：

\[
p_i=[\text{dirty type, dirty format, Baran type, Baran format, support bins}].
\]

向量 L2 归一化，以 cosine distance 做 average linkage；稳定 leaf order 上生成 (k\in\{2,4,8\}) 的 half-stride windows，并显式补齐最后一个 tail window。

### 4.4 Public-FD groups

对于公开 FD (X\rightarrow Y)，在 dirty table 上按 determinant value 建立违反组件。组件中 sampled targets 的数量达到候选大小时，在组件 bucket 内使用组合向量排序并取窗口。

### 4.5 Semantic groups

按 target column 分 bucket，从 masked row text 与 schema/context token 构造 semantic sparse vector，再执行与 pattern view 相同的 deterministic average-linkage 和窗口生成。

### 4.6 去重与不可变性

同一公开 FD 的重叠组件可能产生相同物理请求。系统用 `query_id` 检查身份冲突，并以 `prompt_hash` 去重完全相同的请求。每个 action 冻结：

```text
query_id, suite, dataset, arm, group_view,
cell_ids, group_size,
prompt_schema_version, prompt_information_policy,
messages, prompt_hash,
estimated_prompt_tokens, completion_token_ceiling,
estimated_total_tokens, group_features
```

动作生成后不得更换 cells、messages 或 completion ceiling。

## 5. Prompt

### 5.1 信息策略

固定策略为：

```text
prompt_information_policy = dirty_evidence_only_no_baran
prompt_schema_version = bgr-no-baran-v1
```

`information_policy` 进入 query/cache 指纹，但不作为自然语言消息发送。序列化后递归扫描 Baran、clean、correctness、error annotation 等禁止字段。

### 5.2 Query identity

令 canonical JSON 序列化使用 UTF-8、key sorting 和紧凑 separators，则：

\[
\text{query\_id}=\texttt{bgrq\_}\|\text{SHA256}(
\text{suite,dataset,arm,view,ordered cells,schema,policy}).
\]

`prompt_hash` 绑定 schema、policy、完整 messages 与 max completion tokens。Provider checkpoint 进一步绑定 run fingerprint、model、temperature、query ID 和 prompt hash。

### 5.3 Prompt payload

每个 query 的 user payload 包含：

- dataset suite/name/columns；
- group view、size 和有序 cell IDs；
- 与目标相关的公开 FD components；
- 每个 target 的 dirty value、masked dirty row、column profile 和最多 5 个 similar dirty rows；
- `propose | abstain` 输出要求。

每个 target 独立返回一个 repair item。缺项、重复 cell ID、未知 cell ID、非法 decision、非字符串 repair、query ID 不匹配和 provider failure 均进入解析审计。

### 5.4 Completion ceiling

冻结公式为：

\[
M(k)=
\begin{cases}
192,&k=1,\\
64+192k,&k>1.
\end{cases}
\]

## 6. 实验二的冻结 partitions

### 6.1 Primary structured partition

正式实验固定 (k=4)。候选排序键为：

\[
(\text{view priority},-\text{cohesion},\text{query ID}),
\]

其中 view priority 按配置顺序：

```text
row → pattern → public_fd → semantic
```

按排序顺序贪心加入与已选 cells 不重叠的 group，得到互斥 structured partition。该 partition 只用于实验比较，不替代完整 BGR 的重叠动作库。

### 6.2 Matched random partition

Random arm 必须覆盖与 structured arm 完全相同的 cells，并保持 group-size multiset。对每个 structured cell，按以下 strata 做 seed 44 的稳定 rotation：

```text
(origin structured view, target column, singleton payload-length quartile)
```

置换后的 cell 填回原 structured group 槽位。每个 random query 使用相同 Prompt builder，并保留来源 structured query ID、matched view、group size 和 length-bin multiset。无法置换的固定槽位和 exact group collision 必须审计。

## 7. 响应解析与完整分母

对于 query (q)，每个 (i\in G_q) 必须产生一条 cell-level 记录。若 provider 失败、JSON 无法解析、item 缺失或 item 非法，则：

- `valid_prediction = false`；
- `correct = false`；
- 该 cell 仍保留在分母；
- 最终部署路径回退到 Baran。

`abstain` 同样按未由 LLM 修对处理。实验 accuracy 衡量 LLM arm 本身，不用 fallback 把失败隐藏掉。

## 8. 实验一：互补性

对每个 sampled cell，令：

\[
B_i=\mathbf 1[b_i=y_i],\qquad L_i=\mathbf 1[\hat y_i^{single}=y_i].
\]

四格表：

\[
n_{11}=\sum B_iL_i,\quad
n_{10}=\sum B_i(1-L_i),\quad
n_{01}=\sum(1-B_i)L_i,\quad
n_{00}=\sum(1-B_i)(1-L_i).
\]

Oracle upper bound：

\[
UB=\frac{n_{11}+n_{10}+n_{01}}{N}.
\]

报告 Baran accuracy、singleton accuracy、UB、UB−Baran、UB−Best、salvage/rescue、precision、recall 和 F1。Bootstrap 以 row 为 cluster，2,000 次，seed 45；逐数据集 McNemar 使用 exact test，并对 9 个数据集做 Holm 校正。

## 9. 实验二：分组质量与成本

实验二直接复用实验一 canonical singleton ledger，不重复请求。只对 structured/random 的共同覆盖 population 做三臂比较：

\[
\Delta_{S-1}=Acc_{structured}-Acc_{singleton},
\]

\[
\Delta_{S-R}=Acc_{structured}-Acc_{random}.
\]

structured−singleton 按 structured query cluster bootstrap；random−singleton 按 random query；structured−random 使用双向 query-cluster bootstrap。报告 per-dataset、macro、micro 和 worst dataset。

Token saving 定义为：

\[
Saving=1-\frac{tokens/cell_{structured}}{tokens/cell_{singleton}}.
\]

只用 provider 实际 usage 计算主指标；未知 usage 不以估算值冒充。成本审计另报告 observed tokens、unknown attempts 的保守估计、provider attempts、失败次数和延迟。

## 10. Uplift gate

对 (i\in G_q)，定义：

\[
h_{iq}=\mathbf 1[B_i=0,L_{iq}=1],\qquad
r_{iq}=\mathbf 1[B_i=1,L_{iq}=0].
\]

分别估计：

\[
\hat p^+_{iq}=P(h_{iq}=1\mid z_{iq}),\qquad
\hat p^-_{iq}=P(r_{iq}=1\mid z_{iq}),
\]

其中 (z_{iq}) 包含 cell、group 和交互特征，但不能包含 clean value 或由其直接导出的在线字段。原始净收益：

\[
\hat u_{iq}=\hat p^+_{iq}-\rho\hat p^-_{iq}.
\]

加入不确定性惩罚后：

\[
\ell_{iq}=\max\{0,\hat u_{iq}-\gamma\sigma_{iq}\}.
\]

训练和验证使用 TableEG family holdout/cross-fitting，防止同 family 泄漏。Routeability AUPRC、Brier 与 top-ranked observed uplift 作为诊断指标报告，但不决定是否执行 Phase 3。

## 11. 次模 knapsack

定义 cell 当前由集合 (S) 获得的价值：

\[
m_i(S)=\max\left(0,\max_{q\in S:i\in G_q}\ell_{iq}\right).
\]

总目标：

\[
F(S)=\sum_iw_im_i(S).
\]

### 11.1 单调性

若 (A\subseteq B)，则对每个 cell 有 (m_i(A)\le m_i(B))，所以 (F(A)\le F(B))。

### 11.2 次模性

新增 query (q) 的边际收益为：

\[
\Delta(q\mid S)=\sum_{i\in G_q}w_i\max(0,\ell_{iq}-m_i(S)).
\]

当 (A\subseteq B) 时 (m_i(A)\le m_i(B))，故：

\[
\Delta(q\mid A)\ge\Delta(q\mid B).
\]

因此 (F) 满足 diminishing returns，是单调次模函数。

### 11.3 优化器

在 (sum_{q\in S}c_q\le B) 下，用 lazy marginal-gain-per-cost greedy：

\[
q^*=\arg\max_{q\notin S}\frac{\Delta(q\mid S)}{c_q}.
\]

最终在 greedy solution 与 best feasible singleton action 中取价值更高者；小规模实例用 exhaustive optimum 校验实现。

## 12. 外部 verifier 与 fallback

LLM response 冻结后，verifier 才能同时观察 Baran candidate、LLM candidate、dirty table 和公开约束。它只允许三种结果：

```text
accept_llm | keep_baran | reject
```

任何 parse failure、missing item、未知 target、约束冲突恶化或低置信情况默认 `keep_baran`。这使实验中的 LLM 风险被完整记录，而部署结果仍具有保守 fallback。

## 13. Router-v2 门禁与 v1 历史规则

冻结的 v1 初步实验采用过三项效果 hard gate；因此 v1 中 grouping 未通过时没有执行完整 BGR。Router-v2 是新的、独立绑定的正式 protocol revision，不回改 v1 结果，也不再把以下效果指标作为训练或 Phase 3 的前置条件：

```text
complementarity_supported
grouping_supported
routeability_supported
```

Router-v2 的 hard gates 仅包括：数据与 family split 零泄漏、预算不超限、模型身份一致、Prompt 递归信息边界、request identity 与 run fingerprint 一致，以及每个方法切片完整覆盖指定的 22,198 cells。任何一项失败都停止后续 API 调度或拒绝完成 run。

## 14. 理论边界

- 次模性建立在冻结、非负的 (ell_{iq}) 上；若选择后动态改 Prompt 或重新估计收益，理论不再直接成立；
- Oracle UB 依赖 clean label，只是离线上界；
- candidate groups 可以重叠，但一次正式 action 的 cells 和成本不可变；
- Baran 外部特征可以影响分组和路由，但不能穿过 Prompt 信息边界；
- A/B/C 是经验门禁，不是次模优化近似保证的一部分。

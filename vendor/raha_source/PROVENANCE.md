# Baran/RAHA vendored source provenance

The `raha/` package in this directory is a byte-for-byte source snapshot of:

`project/DataBase/DataCleaning_KDD/data_cleaning_baseline/raha/raha/`

Snapshot date: 2026-07-18 (Asia/Hong_Kong).

SHA-256 tree digest over sorted `.py` files, including relative paths:

`02d936f520e419d4a6ee96928a02e429630c9cc103350654bb2d15047c769da2`

The upstream `LICENSE.md`, README, and requirements file are retained beside the package. Historical `runs/`, datasets, git metadata, caches, notebooks, and multi-gigabyte optional supplementary knowledge bases are intentionally not vendored. The correction configuration used by this project leaves `PRETRAINED_VALUE_BASED_MODELS_PATH` empty, so those optional assets are not read by the formal experiment.

